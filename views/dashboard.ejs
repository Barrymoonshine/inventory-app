<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/head.ejs')%>
  <body>
    <%- include('./partials/nav.ejs')%>
    <div class="dashboard-container">
      <div>Total Products</div>
      <div>Total Store Value</div>
      <div>Out of Stock</div>
      <div>All Categories</div>
      Prod details to go here:
      <div class="products-content">
        <% if(products.length >0 ) { %> <% products.map((product) => { %>
          <h3 class="name"><%=product.name%></h3>
          <p class="sku"><%=product.sku%></p>
          <p class="cost"><%=`£${product.cost.toFixed(2)}`%></p>
          <p class="quantity"><%=product.quantity%></p>
          <p class="total-value"><%=`£${(product.quantity*product.cost).toFixed(2)}`%></p>
          <p class="stock-status"><%=product.inStock%></p>
          <p class="category"><%=product.category%></p>
          <button>edit</button>
          <button>delete</button>
          <button>view</button>
        </a>
        <%})} else {%>
        <p>No products saved</p>
        <%}%>
    </div>
    <%- include('./partials/footer.ejs')%>
  </body>
</html>
